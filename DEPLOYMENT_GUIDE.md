# دليل رفع الموقع على الاستضافة

## 📦 الملفات المطلوبة للرفع

يجب رفع **محتويات مجلد `dist/` فقط** على الاستضافة.

### محتويات مجلد dist:
```
dist/
├── index.html          (الصفحة الرئيسية)
├── assets/            (ملفات JavaScript و CSS)
├── manifest.json      (ملف PWA)
├── robots.txt         (ملف SEO)
├── sitemap.xml        (خريطة الموقع)
├── _headers           (إعدادات CORS)
└── _redirects         (إعادة توجيه للصفحات)
```

---

## 📋 خطوات الرفع على الاستضافة

### الخطوة 1: تحضير الملفات

1. **حمّل مجلد المشروع** على جهازك
2. **افتح مجلد `dist/`**
3. **حدد جميع الملفات والمجلدات داخله** (لا ترفع مجلد dist نفسه!)

### الخطوة 2: رفع الملفات عبر cPanel

#### A. استخدام File Manager في cPanel:

1. **سجل دخول إلى cPanel**
2. **افتح File Manager**
3. **اذهب إلى مجلد `public_html`**
4. **احذف أي محتوى قديم** (index.html القديم وغيره)
5. **ارفع جميع الملفات من داخل مجلد `dist/`**:
   - اضغط **Upload**
   - اختر جميع الملفات والمجلدات من `dist/`
   - انتظر حتى يكتمل الرفع
6. **تأكد من البنية النهائية:**
   ```
   public_html/
   ├── index.html
   ├── assets/
   ├── manifest.json
   ├── robots.txt
   ├── sitemap.xml
   ├── _headers
   └── _redirects
   ```

#### B. أو استخدام FTP (FileZilla):

1. **افتح FileZilla** أو أي برنامج FTP
2. **اتصل بالاستضافة:**
   - Host: `ftp.servikw.com`
   - Username: (اسم المستخدم من لوحة الاستضافة)
   - Password: (كلمة المرور)
   - Port: 21
3. **اذهب إلى مجلد `public_html`**
4. **احذف المحتوى القديم**
5. **اسحب جميع الملفات من مجلد `dist/`** إلى `public_html`

---

## 🔧 إعداد متغيرات البيئة

**مهم جداً:** المتغيرات موجودة بالفعل في الكود المبني، لكن إذا أردت تغييرها مستقبلاً:

### الطريقة الأولى: إنشاء ملف .env (اختياري)

إذا أردت إضافة ملف `.env` في `public_html`:

```env
VITE_SUPABASE_URL=https://vhymurviqcaszlrjcgce.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZoeW11cnZpcWNhc3pscmpjZ2NlIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQ1NzI2MzYsImV4cCI6MjA4MDE0ODYzNn0.tg-ni9oyKO_BWlNdZfPVdZez8ocSWMRnm4Lu7WwX0O4
```

**ملاحظة:** هذا الملف **ليس ضرورياً** لأن القيم موجودة في الكود المبني!

---

## 🔒 إعداد HTTPS

### إذا كانت الاستضافة تدعم SSL:

1. **في cPanel:**
   - اذهب إلى **SSL/TLS Status**
   - اضغط **Install SSL Certificate** بجانب `servikw.com`
   - اختر **Let's Encrypt** (مجاني)
   - اضغط **Install**

2. **تفعيل إجبار HTTPS:**
   - في File Manager، افتح ملف `.htaccess` في `public_html`
   - إذا لم يكن موجوداً، أنشئه وأضف هذا الكود:

```apache
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

---

## ✅ التحقق من التثبيت

بعد رفع الملفات:

1. **افتح المتصفح واذهب إلى:** `https://servikw.com`
2. **تأكد من:**
   - ✅ الصفحة الرئيسية تظهر بشكل صحيح
   - ✅ يمكنك التسجيل والدخول
   - ✅ يمكنك إنشاء طلب خدمة
   - ✅ لا توجد أخطاء في Console (F12)

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: صفحة فارغة بيضاء
**الحل:**
- افتح F12 → Console وشاهد الأخطاء
- تأكد من رفع **جميع** الملفات من `dist/`
- تأكد من وجود مجلد `assets/`

### المشكلة 2: الصفحات لا تعمل (404 Error)
**الحل:**
- أضف ملف `.htaccess` في `public_html`:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

### المشكلة 3: الموقع لا يتصل بـ Supabase
**الحل:**
- افتح Console (F12) وشاهد الأخطاء
- تأكد من أن الاستضافة لا تحجب طلبات API الخارجية
- تواصل مع دعم الاستضافة إذا استمرت المشكلة

### المشكلة 4: الخطوط العربية لا تظهر
**الحل:**
- تأكد من رفع جميع ملفات CSS في مجلد `assets/`
- تأكد من أن الاستضافة تدعم HTTPS (للخطوط من Google Fonts)

---

## 📁 ملخص البنية النهائية

```
public_html/
├── index.html                    (الصفحة الرئيسية)
├── assets/
│   └── index-DYDSPClf.js        (كود JavaScript المبني)
├── manifest.json                 (PWA)
├── robots.txt                    (SEO)
├── sitemap.xml                   (SEO)
├── _headers                      (CORS)
├── _redirects                    (Routing)
└── .htaccess                     (اختياري - لإعادة التوجيه)
```

---

## 🎯 ملاحظات مهمة

1. ✅ **Backend موجود بالفعل:** كل البيانات على Supabase، لا تحتاج تثبيت backend!
2. ✅ **المتغيرات موجودة:** في الكود المبني، لا تحتاج ملف .env
3. ✅ **الموقع Static:** مجرد HTML + JavaScript، سهل الرفع
4. ⚠️ **لا ترفع:** ملفات المشروع الأخرى (src/, node_modules/, إلخ)
5. ⚠️ **حجم الرفع:** حوالي 560 KB فقط!

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تأكد من اتباع الخطوات بالترتيب
2. افحص Console (F12) للأخطاء
3. تأكد من رفع **كل** محتويات dist/
4. تواصل مع دعم الاستضافة إذا كانت المشكلة من الخادم

---

**الموقع جاهز للرفع! 🚀**
